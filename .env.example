# FocusGroup — environment configuration
# Copy this file to .env and fill in your values.
# setup.sh does this automatically on first run.

# ── Local Ollama (persona conversations) ─────────────────────────────────────
# Leave as defaults unless you've changed your Ollama setup.
OLLAMA_MODEL=llama3.1:8b
OLLAMA_BASE_URL=http://localhost:11434

# ── Redis (session history) ───────────────────────────────────────────────────
REDIS_URL=redis://localhost:6379

# ── ChromaDB (persona vector store) ──────────────────────────────────────────
CHROMA_PERSIST_PATH=./.chromadb

# ── Ollama Cloud — image analysis (!image command) ───────────────────────────
# Required only for loading ad images into the room (!image <path>).
# Get your API key at: https://ollama.com/settings/keys
OLLAMA_API_KEY=
OLLAMA_HOST=https://ollama.com
